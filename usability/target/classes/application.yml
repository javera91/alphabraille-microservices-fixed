server:
  port: 8082
spring:
  application:
    name: usability
  datasource:
    url: jdbc:postgresql://localhost:5432/usabilitydb
    username: postgres
    password: your_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
